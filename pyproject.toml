[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pm2-rpc"
version = "0.1.0"
description = "Pure-Python client for PM2's daemon RPC socket"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
authors = [{name = "Paul Soares"}]
keywords = ["pm2", "rpc", "axon", "amp", "process-manager"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Operating System :: POSIX",
    "Topic :: System :: Systems Administration",
    "Typing :: Typed",
]
dependencies = []

[project.urls]
Repository = "https://github.com/cadance-io/pm2-rpc"
Issues = "https://github.com/cadance-io/pm2-rpc/issues"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]

[tool.hatch.build.targets.wheel]
# Flat layout: ship the two top-level modules as the public surface.
# `main.py` is a demo entry point and intentionally excluded.
only-include = ["pm2.py", "axon.py"]
sources = ["."]

[tool.hatch.build.targets.sdist]
only-include = ["pm2.py", "axon.py", "README.md", "LICENSE", "pyproject.toml"]

[tool.pytest.ini_options]
testpaths = ["tests"]
